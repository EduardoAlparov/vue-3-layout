// export default function auth({ next, store, nextMiddleware }) {
//   return nextMiddleware();
// }
/* eslint-disable */
export default function auth({ nextMiddleware }) {
  if (false) {
    return;
  }
  
  return nextMiddleware();
}
